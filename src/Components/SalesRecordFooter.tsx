/*import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

interface IProp {
  label: string;
  value: string;
}

const SalesRecordFooter = (props: IProp): JSX.Element => {
  return <View />;
};

const styles = StyleSheet.create({});*/
